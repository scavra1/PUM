<UserControl
    x:Class="PUM.MobileApp.CustomControls.ReservationControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PUM.MobileApp.CustomControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid x:Name="mainGrid">
        <TextBlock FontSize="20" Height="30" Text="{Binding Reservation.Person}" HorizontalTextAlignment="Center" Foreground="Black">
            <TextBlock.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem Text="Zarezerwuj" Command="{Binding ReserveCommand}" CommandParameter="{Binding Reservation}"/>
                    <MenuFlyoutItem Text="Usuń rezerwację" Command="{Binding DeleteReservationCommand}" CommandParameter="{Binding Reservation}"/>
                </MenuFlyout>
            </TextBlock.ContextFlyout>
        </TextBlock>
    </Grid>
</UserControl>
